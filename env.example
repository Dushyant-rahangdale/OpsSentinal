DATABASE_URL=postgresql://opssentinal:your_secure_password@localhost:5432/opssentinal_db?sslmode=prefer

# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key-here

# Cron Job Authentication
CRON_SECRET=your-cron-secret-key-here

# Email Configuration (Optional - Configure via UI at Settings → System → Notification Providers)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASSWORD=your-password
# SMTP_FROM=noreply@example.com

# Twilio Configuration (Optional - Configure via UI)
# TWILIO_ACCOUNT_SID=your-account-sid
# TWILIO_AUTH_TOKEN=your-auth-token
# TWILIO_PHONE_NUMBER=+1234567890

# Push Notification Configuration (Optional - Configure via UI)
# ONESIGNAL_APP_ID=your-app-id
# ONESIGNAL_API_KEY=your-api-key

# AWS SNS Configuration (Optional - Configure via UI)
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key

# PostgreSQL Configuration (for Docker Compose)
POSTGRES_USER=opssentinal
POSTGRES_PASSWORD=devpassword
POSTGRES_DB=opssentinal_db
