apiVersion: v1
kind: Service
metadata:
  name: opsguard-service
  namespace: opsguard
  labels:
    app: opsguard-app
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: opsguard-app
