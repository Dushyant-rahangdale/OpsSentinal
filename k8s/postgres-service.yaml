apiVersion: v1
kind: Service
metadata:
  name: opssure-postgres-service
  namespace: opssure
  labels:
    app: opssure-postgres
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
  selector:
    app: opssure-postgres

