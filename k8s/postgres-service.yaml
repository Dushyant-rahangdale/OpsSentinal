apiVersion: v1
kind: Service
metadata:
  name: opssentinal-postgres-service
  namespace: opssentinal
  labels:
    app: opssentinal-postgres
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
  selector:
    app: opssentinal-postgres


