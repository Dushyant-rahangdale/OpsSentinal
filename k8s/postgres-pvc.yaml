apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: opsguard-postgres-pvc
  namespace: opsguard
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  # Uncomment and configure for specific storage class
  # storageClassName: standard
