apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opssentinal.fullname" . }}-test-connection"
  labels:
    {{- include "opssentinal.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opssentinal.fullname" . }}:{{ .Values.service.port }}/api/health']
  restartPolicy: Never
