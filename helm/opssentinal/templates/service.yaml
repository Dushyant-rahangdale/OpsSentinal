apiVersion: v1
kind: Service
metadata:
  name: {{ include "opssentinal.fullname" . }}
  labels:
    {{- include "opssentinal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opssentinal.selectorLabels" . | nindent 4 }}
